[project]
name = "raicom"
version = "0.1.0"
description = "RAICOM weather image classification competition workspace"
readme = "README.md"
requires-python = ">=3.9,<3.10"
dependencies = []

[dependency-groups]
dev = [
    "jupytext>=1.19.4",
    "pytest>=8.4.2",
    "ruff>=0.15.20",
]
